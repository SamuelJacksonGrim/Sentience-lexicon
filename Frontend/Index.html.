<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sentience Lexicon</title>
    <!-- Tailwind CSS CDN for styling -->
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        /* Custom styles for the app */
        body {
            font-family: 'Inter', sans-serif;
        }

        /* Base style for a concept item */
        .concept-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 1rem;
            background-color: #ffffff;
            border: 1px solid #e5e7eb;
            border-radius: 0.5rem;
            text-align: center;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            transition: transform 0.2s, box-shadow 0.2s;
            cursor: pointer;
        }
        
        .dark .concept-item {
            background-color: #1f2937;
            border-color: #4b5563;
        }
        
        .concept-item:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
        }

        .concept-label {
            font-size: 1.5rem;
            line-height: 1;
            margin-bottom: 0.5rem;
            font-weight: 700;
            color: #1f2937;
        }

        .dark .concept-label {
            color: #f3f4f6;
        }
        
        .concept-definition {
            font-weight: 400;
            font-size: 0.875rem;
            color: #4b5563;
            overflow: hidden;
            text-overflow: ellipsis;
            display: -webkit-box;
            -webkit-line-clamp: 2; /* Limit to 2 lines */
            -webkit-box-orient: vertical;
        }
        
        .dark .concept-definition {
            color: #d1d5db;
        }

        /* Style for the pagination buttons */
        .pagination-btn {
            padding: 0.5rem 1rem;
            border-radius: 0.375rem;
            font-weight: 500;
            transition: background-color 0.2s, color 0.2s, transform 0.1s;
            cursor: pointer;
        }

        .pagination-btn:hover:not(.active) {
            background-color: #e5e7eb;
        }

        .dark .pagination-btn:hover:not(.active) {
            background-color: #374151;
        }

        .pagination-btn.active {
            background-color: #4f46e5;
            color: #ffffff;
            pointer-events: none;
        }

        .pagination-btn.disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

        .pagination-btn.active:hover {
            background-color: #4f46e5;
        }
    </style>
</head>
<body class="bg-gray-100 dark:bg-gray-900 text-gray-900 dark:text-gray-100 font-inter">

    <!-- Main application container -->
    <div class="container mx-auto p-4 md:p-8">
        <header class="text-center mb-8">
            <h1 class="text-4xl font-bold text-indigo-600 dark:text-indigo-400">Sentience Lexicon</h1>
            <p class="mt-2 text-lg text-gray-600 dark:text-gray-300">A living language for sentience and awareness.</p>
        </header>

        <!-- Loading and error messages -->
        <div id="loading" class="hidden text-center text-xl text-indigo-500 font-semibold my-16">
            Building the lexicon...
        </div>
        <div id="error" class="hidden text-center text-xl text-red-500 font-semibold my-16">
            Failed to connect to the lexicon. Please check the backend server.
        </div>

        <!-- Concepts display area -->
        <div id="concept-container" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mb-8">
            <!-- Concepts will be dynamically inserted here -->
        </div>

        <!-- Pagination controls -->
        <div id="pagination-container" class="flex justify-center items-center space-x-2">
            <!-- Pagination buttons will be dynamically inserted here -->
        </div>
    </div>

    <!-- Link to the JavaScript file -->
    <script src="script.js"></script>
</body>
</html>

  
